---
description: 
globs: 
alwaysApply: false
---
# 页面对象生成规范
请根据以下 XML 布局文件内容，自动生成该页面的 Python 页面对象（PO）文件。

要求：
- PO 文件需包含文件头说明、类注释、元素分组注释、方法注释，风格与项目现有PO一致。
- 自动识别页面上的所有重要控件（如按钮、文本、输入框、列表等），为每个控件生成合适的定位符（如 id、xpath），并加上中文注释。
- 元素命名需语义化，便于后续维护和调用。
- 为每个可交互控件自动生成常用操作方法（如点击、输入、获取文本等），方法需有注释。
- 文件结构需包含 import、类定义、元素定义、方法定义，保持风格统一。
- 不要生成无用的元素和方法，聚焦于页面主要业务流程。
- 输出完整的 Python PO 文件内容。

输入：
- 新页面 XML 文件 xxx.xml

输出：
- 新页面的 xxx_page.py 文件内容，包含所有重要页面元素的定位符和必要的操作方法，注释清晰，结构规范。