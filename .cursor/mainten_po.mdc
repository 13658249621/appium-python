---
description: 
globs: 
alwaysApply: false
---
# xxx_page.py 页面对象补全规范

## 场景描述

请根据以下 XML 布局文件内容，分析页面上所有重要的控件（如按钮、文本、输入框等），并将缺失的元素以合适的定位方式（如 id、xpath）补充到指定的 PO 文件（xxx_page.py）中。

## 要求
- 保持 PO 文件原有结构和风格，新增元素需加上中文注释，便于理解。
- 元素命名需语义化，便于后续维护和调用。
- 如有必要，可补充对应的操作方法（如点击、输入、获取文本等）。
- 不要删除原有的元素和方法，只做补充和完善。

## 输入
- 现有 PO 文件 xxx_page.py
- 页面 XML 文件 意向沟通牛人详情页.xml

## 输出
- 补全后的 xxx_page.py 文件内容，包含所有重要页面元素的定位符和必要的操作方法，注释清晰。
